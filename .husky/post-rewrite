#!/bin/bash
# >>> 5key
# 5key post-rewrite hook: transfer AI attribution when commits are amended or rebased.
# Git pipes "old-sha new-sha" lines on stdin. FAIL-OPEN: must never block.
# For questions or issues, visit: https://pandora.tyinternal.com/qa/pandora
trap 'exit 0' ERR
set +e
/usr/local/bin/5key on-rewrite --repo "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
# <<< 5key
